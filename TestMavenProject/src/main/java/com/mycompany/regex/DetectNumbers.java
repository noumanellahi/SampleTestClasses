package com.mycompany.regex;

public class DetectNumbers {
	public static void main(String[] args) {
		String elementText = "1";
//		if (elementText.matches("-?\\d+(\\.\\d+)?") || elementText.matches(".*\\d.*")) {
//			if (element.getTagName().equals("a") && elementText.contains(String.valueOf(pageCount + 1))) {
//				tmpNextButton = element;
//			}
//		}
	}
}
